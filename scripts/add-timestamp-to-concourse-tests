#!/bin/sh

set -eux

timestamp="$(date)"

echo "$timestamp" >> $OUTPUT_FILE

cd concourse-tests-dirty

git config --global user.email "autobot@concourse.ci"
git config --global user.name "Concourse"

git add .

git commit -m "Autocommit from Concourse"

